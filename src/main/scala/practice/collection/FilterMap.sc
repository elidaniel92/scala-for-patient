/**
 * flatten
 * flatMap vs collect vs filterWith
 *
 * mapValues => deprecated
 */

// filterWith == filter().map()

// collect = { filter().map(), filter().map(), filter().map() }

// https://stackoverflow.com/questions/36961777/scala-can-there-be-any-reason-to-prefer-filtermap-over-collect

